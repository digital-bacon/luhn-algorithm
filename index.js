/**
 * A function that, when given a number, returns whether it is valid or
 * not (according to the Luhn Algorithm)
 * @param {number} number - The number to check
 * @returns {boolean} Will return `true` if the given number is valid
 */
const check = (number) => {
  // Not yet implemented
};

module.exports = check;